# Base URL for the Nest API (must include `/api` global prefix).
# Backend default: http://localhost:3000 — dashboard dev server uses port 5173 to avoid clashing.
VITE_API_URL=http://localhost:3000/api
