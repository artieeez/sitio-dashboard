/**
 * Left pane for school-scoped hub (`/schools/$schoolId`): school “home” surface (minimal until a hub exists).
 */
export function SchoolHomeListPane() {
  return <div className="min-h-0 min-w-0 flex-1" />;
}
